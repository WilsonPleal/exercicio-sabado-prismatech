let frutas = ["maça", "banana", "manga", "abacaxi", "abacate"];
console.log(frutas);
for (let i = 0; i < frutas.length; i++) {
  // Usa-se o LENGHT paraverificar a quantidade de frutas sem precisar alterar o valor em si
  console.log(frutas[i]);
}

let soma = [2, 3, 4, 5, 8];
console.log(soma);
let valor = 0;
for (let i = 0; i < soma.length; i++) {
  valor += soma[i];
}
console.log(valor);

let numero = [1, 21, 3, 56, 98, 60];
console.log(numero);
let maior = 0;
for (let i = 0; i < maior.length; i++) {
  if (numero[i] > maior) {
  }
}
console.log(maior);
